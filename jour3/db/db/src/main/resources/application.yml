spring:
  datasource:
    url: jdbc:h2:mem:dbruntrack
    driver-class-name: org.h2.Driver
    username: root
    password: root
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

